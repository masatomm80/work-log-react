# CLAUDE.md

このファイルはClaude Code向けの簡潔な案内板です。詳細な仕様や経緯は
既存ドキュメントに書かれているため、ここでは重複して書き直しません。
作業前に該当ドキュメントを必ず確認してください。

## プロジェクト名

Masato Taxi AI

## 現在のバージョン

**Ver.1.0**（安定版として確定済み）。大きな仕様変更はここで一旦区切り、以降の機能追加はVer.1.1以降として扱う。
Ver.1.0確定時点の詳細は [docs/SPECIFICATION.md](docs/SPECIFICATION.md) を参照。

## 詳細仕様の参照先

- [docs/SPECIFICATION.md](docs/SPECIFICATION.md) — コードを根拠とした現在の仕様、未実装・要確認事項
- [README.md](README.md) — 構成、機能一覧、セットアップ、デプロイ手順
- `src/WorkLog.jsx` — アプリ本体の実装（画面・状態管理・保存・集計・CSV・バックアップ）
- `package.json` — 依存パッケージ、スクリプト

上記に書かれていない、または他の資料（CHANGELOG.md、ROADMAP.md、IDEAS.md、
docs/DECISIONS.md など）がプロジェクトに追加された場合は、そちらも仕様の一次情報として
優先的に確認してください。

## 仕様の優先順位

仕様を確認する際は、以下の優先順位に従うこと（存在するファイルのみが対象）。

1. `docs/DECISIONS.md`
2. `docs/SPECIFICATION.md`
3. `README.md`
4. 現在のコード

**コードと仕様書（上記1〜3）の内容が一致しない場合は、どちらが正しいか勝手に判断して
コードを書き換えず、差異をそのまま報告し、ユーザーに確認を求めること。**

## 作業ルール

1. **作業開始前に必ず `git status` と `git diff` を確認する。**
   未コミットの変更や他のAI（例：GitHub Copilot）による途中変更がないか把握してから着手する。
2. **指示された箇所以外を変更しない。** 関係のないリファクタや整形は行わない。
3. **未コミットの変更や他のAIの途中変更を勝手に破棄・上書きしない。**
   不要に見えても、まず内容を確認し必要ならユーザーに確認する。
4. **不明な仕様は推測しない。** まず既存資料（README.mdほか関連ドキュメント）を確認し、
   それでも不明な場合はユーザーに質問する。
5. **ユーザーの許可なくコミット・プッシュ・デプロイしない。**
6. **作業後は日本語で報告する。** 変更したファイル、変更内容、実施したチェックの結果、
   残課題（あれば）を明記する。
7. **スマートフォン表示を優先する。** 既存の機能・入力項目は削除しない。
8. **「実装済み」「検討中」「将来予定」を混同しない。** 資料に記載がある場合はその区分に従い、
   区分が不明な場合はユーザーに確認する。
